Ideas, Principles:
- start off with wrapping the bundled apis and then consolidate
  APIs to be wrapped:
  - SDL2
  - Nuklear
  - OpenXR
  - DirectX
- get inspiration from Khronos Group APIs, that way people will feel familiar with it
- A prefix is probably necessary to avoid code collision
- "cp_" as API prefix?
- 

//Initialization
cp_init: SDLInit, nk_sdl_init

cp_loop(input_handling()*): nk_poll_event, sdl_poll_event, xr_poll_event

cp_close: SDL...

Adopt SDLs hint system?

//Errors
cp_get_error: SDL_GetError

cp_set_error: SDL_SetError

//Logging
cp_log: SDL_LogMessage

//Debugging
cp_assert: SDL_assert

//Display and Window Management
cp_create_window: SDL_CreateWindow


//Input Handling

//Audio

//Multi-Threading

//File I/O

//Platform Detection

//Power